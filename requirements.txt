# Backend FastAPI
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Banco de dados
sqlalchemy>=2.0.0
aiosqlite>=0.20.0          # driver assíncrono SQLite (opcional, já funciona síncrono)

# Validação e configuração
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Autenticação
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9    # upload de arquivos (NF em PDF)

# Email
# (usa smtplib da stdlib — sem dependência extra)

# PDF (Fase 4)
reportlab>=4.0.0

# Excel (Fase 4)
# openpyxl>=3.1.0

# Oracle (opcional — descomentar ao migrar)
# oracledb>=2.0.0

# Importação de planilhas (clientes)
pandas>=2.0.0
odfpy>=1.4.1               # leitura de arquivos .ods
openpyxl>=3.1.0            # leitura de arquivos .xlsx/.xlsm

# Frontend PySide6 (Fase 2 — instalar separadamente no cliente)
# PySide6>=6.7.0
# httpx>=0.27.0             # chamadas HTTP ao servidor
# qrcode>=7.4.2             # QR code no formulário
